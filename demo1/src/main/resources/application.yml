# Set the server port to 8888
server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/voters_a
    username: root
    password: pk0101PK
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  application:
    name: voters-api